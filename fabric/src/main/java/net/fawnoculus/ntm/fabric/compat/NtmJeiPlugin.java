package net.fawnoculus.ntm.fabric.compat;

import mezz.jei.api.JeiPlugin;

@JeiPlugin
public class NtmJeiPlugin /* implements IModPlugin */ {
    /* FIXME once Architectury ports to Loom 1.14, because then we can switch to a newer Jei Version, so we can actually use it
    @Override
    public @NotNull Identifier getPluginUid() {
        return Ntm.id("jei_plugin");
    }
    */
}
